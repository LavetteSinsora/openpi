[project]
name = "evdev"
version = "1.9.2"
description = "Stub — evdev is only needed for pynput keyboard teleoperation, unused in openpi training"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/evdev"]
